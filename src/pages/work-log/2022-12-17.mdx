import WorkLogLayout from "@/components/WorkLogLayout";

export const meta = {
  author: "Bow Tied Hamachi",
  date: "2022-12-17",
  title: "",
  description: "Built out a new worklog UI",
  rating: "3",
};

export default ({ children }) => (
  <WorkLogLayout meta={meta}>{children}</WorkLogLayout>
);

- Finished putting together the main skeleton together for the Proof of Work page
- Deployed to Vercel and uploaded older articles so that code is in-line with work
- Added in project description for re-entrancy mock
